"""ORM models for the temples module.

Intentionally empty for now: the Phase-1 temple directory ships as curated
Python seed data (seed_data.py), same as the knowledge module's corpus — no
schema migration needed. When the directory grows past hand-curation (OSM
import / Places cache), promote it to a table here via an Alembic migration,
which requires human approval (AGENTS.md).
"""
